<?php require __DIR__ . "/vendor/autoload.php"; $app = require_once __DIR__ . "/bootstrap/app.php"; $kernel = $app->make(Illuminate\Contracts\Console\Kernel::class); $kernel->bootstrap(); $invs = App\Models\Invitation::all(); foreach($invs as $inv) { echo $inv->email . " - " . $inv->status . "\n"; }
