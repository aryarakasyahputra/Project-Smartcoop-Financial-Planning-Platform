<?php require __DIR__ . "/vendor/autoload.php"; $app = require_once __DIR__ . "/bootstrap/app.php"; $kernel = $app->make(Illuminate\Contracts\Console\Kernel::class); $kernel->bootstrap(); $user = App\Models\User::where("email", "founder@test.com")->first(); if($user) { $pivot = Illuminate\Support\Facades\DB::table("company_user")->where("user_id", $user->id)->first(); if($pivot) { echo "Company ID: " . $pivot->company_id . "\n"; } else { echo "No company pivot found.\n"; } }
